# Postgres connection string (works with Supabase)
# e.g. postgresql://postgres:password@db.host:5432/postgres
DATABASE_URL=postgresql://postgres:password@localhost:5432/jadwal

# CORS origins (comma-separated). Default allows localhost dev ports
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
